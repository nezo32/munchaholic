package dev.munchaholic.core;

/** valueAt, canStep (edges and recovery), displayValue / displayDelta for all three units, translationKey. Phase-0 skeleton: tests are added by Dev A. */
class AttributeSpecTest {
}
