package dev.munchaholic.core;

/** Scripted draws for RollSelector.random (capped reroll, fallback, Nothing) and fixed; a long random walk stays in range. Phase-0 skeleton: tests are added by Dev A. */
class RollSelectorTest {
}
